import {
  Stack,
  Typography,
  Accordion,
  AccordionSummary,
  AccordionDetails,
} from "@mui/material";
import ExpandMoreIcon from "@mui/icons-material/ExpandMore";
const MuiAccordion = () => {
  return (
    <Stack
      sx={{
        maxWidth: "600px",
        margin: "auto",
        paddingTop: "100px",
      }}
    >
      <p>ddd</p>
    </Stack>
  );
};

export default MuiAccordion;
